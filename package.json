{
  "name": "remote-browser",
  "version": "1.0.0",
  "description": "Remote browser accessible via web (Render deploy-ready)",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "postinstall": "npx playwright install"
  },
  "dependencies": {
    "express": "^4.18.2",
    "playwright": "^1.42.0",
    "uuid": "^9.0.0"
  }
}
